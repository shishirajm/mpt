{
  "name": "mpi-website-tests",
  "private": true,
  "description": "Dev-only test tooling for the Mac Precitec India Private Limited site. Not part of the shipped site.",
  "scripts": {
    "test:visual": "playwright test -c tests/visual/playwright.config.js",
    "test:visual:update": "playwright test -c tests/visual/playwright.config.js --update-snapshots=all",
    "test:links": "playwright test -c tests/links/playwright.config.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
